APP_ENV=development
HTTP_PORT=8080
SHUTDOWN_TIMEOUT=15s

AUTH_SERVICE_GRPC_ADDR=localhost:50051
BLOG_SERVICE_GRPC_ADDR=localhost:50051

# Same value as the frontend OWNER_STUDIO_SECRET — required for studio writes.
OWNER_STUDIO_SECRET=marketing-digest-dev

CORS_ALLOWED_ORIGINS=*
